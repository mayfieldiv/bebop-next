// This file is @generated by bebopc-gen-rust. DO NOT EDIT.
// source: /Users/mayfield/vendor/bebop-next/bebop/schemas/bebop/descriptor.bop
// bebopc 2026.0.0-alpha.2
// edition 2026
//
// This file requires the Bebop runtime library.
// https://github.com/6over3/bebop
//
// SPDX-License-Identifier: Apache-2.0
// Copyright (c) 6OVER3 INSTITUTE

#![allow(warnings)]
#![no_implicit_prelude]

extern crate alloc;
extern crate bebop_runtime;
extern crate core;
use alloc::{borrow, boxed, string, vec};
use bebop_runtime as bebop;
use bebop_runtime::DecodeContext as _;
use core::convert::Into as _;
use core::iter::{IntoIterator as _, Iterator as _};
use core::{convert, default, iter, mem, ops, option, result};

// @@bebop_insertion_point(imports)

/// Scalar and compound type kinds.
/// Scalars (1-18) encode as fixed-size little-endian bytes. `BOOL` is 1 byte
/// (0x00 = false, nonzero = true). `INT16` is 2 bytes. `UUID` is 16 bytes.
/// Compound types (19-23) use additional TypeDescriptor fields:
/// - `ARRAY`: element type in `array_element`
/// - `FIXED_ARRAY`: element type + size in `fixed_array_element`, `fixed_array_size`
/// - `MAP`: key and value types in `map_key`, `map_value`
/// - `DEFINED`: referenced type FQN in `defined_fqn`
/// Value 0 is a sentinel. A valid TypeDescriptor never has `kind == UNKNOWN`.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum TypeKind {
  Unknown = 0,
  Bool = 1,
  Byte = 2,
  Int8 = 3,
  Int16 = 4,
  Uint16 = 5,
  Int32 = 6,
  Uint32 = 7,
  Int64 = 8,
  Uint64 = 9,
  Int128 = 10,
  Uint128 = 11,
  Float16 = 12,
  Float32 = 13,
  Float64 = 14,
  Bfloat16 = 15,
  String = 16,
  Uuid = 17,
  Timestamp = 18,
  Duration = 19,
  Array = 20,
  FixedArray = 21,
  Map = 22,
  Defined = 23,
}

impl convert::TryFrom<u8> for TypeKind {
  type Error = bebop::DecodeError;
  fn try_from(value: u8) -> result::Result<Self, bebop::DecodeError> {
    match value {
      0 => result::Result::Ok(Self::Unknown),
      1 => result::Result::Ok(Self::Bool),
      2 => result::Result::Ok(Self::Byte),
      3 => result::Result::Ok(Self::Int8),
      4 => result::Result::Ok(Self::Int16),
      5 => result::Result::Ok(Self::Uint16),
      6 => result::Result::Ok(Self::Int32),
      7 => result::Result::Ok(Self::Uint32),
      8 => result::Result::Ok(Self::Int64),
      9 => result::Result::Ok(Self::Uint64),
      10 => result::Result::Ok(Self::Int128),
      11 => result::Result::Ok(Self::Uint128),
      12 => result::Result::Ok(Self::Float16),
      13 => result::Result::Ok(Self::Float32),
      14 => result::Result::Ok(Self::Float64),
      15 => result::Result::Ok(Self::Bfloat16),
      16 => result::Result::Ok(Self::String),
      17 => result::Result::Ok(Self::Uuid),
      18 => result::Result::Ok(Self::Timestamp),
      19 => result::Result::Ok(Self::Duration),
      20 => result::Result::Ok(Self::Array),
      21 => result::Result::Ok(Self::FixedArray),
      22 => result::Result::Ok(Self::Map),
      23 => result::Result::Ok(Self::Defined),
      _ => result::Result::Err(bebop::DecodeError::InvalidEnum {
        type_name: "TypeKind",
        value: value as u64,
      }),
    }
  }
}

impl convert::From<TypeKind> for u8 {
  fn from(value: TypeKind) -> u8 {
    value as u8
  }
}

impl TypeKind {
  pub const FIXED_ENCODED_SIZE: usize = 1;
  // @@bebop_insertion_point(enum_scope:TypeKind)
}

impl bebop::BebopEncode for TypeKind {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:TypeKind)
    writer.write_byte(*self as u8);
    // @@bebop_insertion_point(encode_end:TypeKind)
  }

  fn encoded_size(&self) -> usize {
    Self::FIXED_ENCODED_SIZE
  }
}

impl<'buf> bebop::BebopDecode<'buf> for TypeKind {
  #[inline(always)]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:TypeKind)
    let value = reader.read_byte()?;
    // @@bebop_insertion_point(decode_end:TypeKind)
    convert::TryFrom::try_from(value)
  }
}

/// Named definition kinds.
/// Each DefinitionDescriptor has one kind, which determines which body field
/// is populated. A definition with `kind == STRUCT` has `struct_def` set;
/// other body fields are absent.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum DefinitionKind {
  Unknown = 0,
  Enum = 1,
  Struct = 2,
  Message = 3,
  Union = 4,
  Service = 5,
  Const = 6,
  Decorator = 7,
}

impl convert::TryFrom<u8> for DefinitionKind {
  type Error = bebop::DecodeError;
  fn try_from(value: u8) -> result::Result<Self, bebop::DecodeError> {
    match value {
      0 => result::Result::Ok(Self::Unknown),
      1 => result::Result::Ok(Self::Enum),
      2 => result::Result::Ok(Self::Struct),
      3 => result::Result::Ok(Self::Message),
      4 => result::Result::Ok(Self::Union),
      5 => result::Result::Ok(Self::Service),
      6 => result::Result::Ok(Self::Const),
      7 => result::Result::Ok(Self::Decorator),
      _ => result::Result::Err(bebop::DecodeError::InvalidEnum {
        type_name: "DefinitionKind",
        value: value as u64,
      }),
    }
  }
}

impl convert::From<DefinitionKind> for u8 {
  fn from(value: DefinitionKind) -> u8 {
    value as u8
  }
}

impl DefinitionKind {
  pub const FIXED_ENCODED_SIZE: usize = 1;
  // @@bebop_insertion_point(enum_scope:DefinitionKind)
}

impl bebop::BebopEncode for DefinitionKind {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:DefinitionKind)
    writer.write_byte(*self as u8);
    // @@bebop_insertion_point(encode_end:DefinitionKind)
  }

  fn encoded_size(&self) -> usize {
    Self::FIXED_ENCODED_SIZE
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DefinitionKind {
  #[inline(always)]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:DefinitionKind)
    let value = reader.read_byte()?;
    // @@bebop_insertion_point(decode_end:DefinitionKind)
    convert::TryFrom::try_from(value)
  }
}

/// Service method streaming modes.
/// Corresponds to `stream` keyword placement in method declarations:
/// ```bebop
/// Ping(Req): Res;               // UNARY
/// Watch(Req): stream Res;       // SERVER_STREAM
/// Upload(stream Chunk): Res;    // CLIENT_STREAM
/// Chat(stream Msg): stream Msg; // DUPLEX_STREAM
/// ```
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum MethodType {
  Unknown = 0,
  Unary = 1,
  ServerStream = 2,
  ClientStream = 3,
  DuplexStream = 4,
}

impl convert::TryFrom<u8> for MethodType {
  type Error = bebop::DecodeError;
  fn try_from(value: u8) -> result::Result<Self, bebop::DecodeError> {
    match value {
      0 => result::Result::Ok(Self::Unknown),
      1 => result::Result::Ok(Self::Unary),
      2 => result::Result::Ok(Self::ServerStream),
      3 => result::Result::Ok(Self::ClientStream),
      4 => result::Result::Ok(Self::DuplexStream),
      _ => result::Result::Err(bebop::DecodeError::InvalidEnum {
        type_name: "MethodType",
        value: value as u64,
      }),
    }
  }
}

impl convert::From<MethodType> for u8 {
  fn from(value: MethodType) -> u8 {
    value as u8
  }
}

impl MethodType {
  pub const FIXED_ENCODED_SIZE: usize = 1;
  // @@bebop_insertion_point(enum_scope:MethodType)
}

impl bebop::BebopEncode for MethodType {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:MethodType)
    writer.write_byte(*self as u8);
    // @@bebop_insertion_point(encode_end:MethodType)
  }

  fn encoded_size(&self) -> usize {
    Self::FIXED_ENCODED_SIZE
  }
}

impl<'buf> bebop::BebopDecode<'buf> for MethodType {
  #[inline(always)]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:MethodType)
    let value = reader.read_byte()?;
    // @@bebop_insertion_point(decode_end:MethodType)
    convert::TryFrom::try_from(value)
  }
}

/// Definition visibility.
/// Controls whether a definition is accessible from importing schemas.
/// Top-level definitions default to `EXPORT`. Nested definitions default
/// to `LOCAL` (visible only within the parent scope).
/// The `export` keyword on a nested definition makes it reachable as
/// `Parent.Child` from other schemas. Local definitions still appear in
/// descriptors because exported types may reference them.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Visibility {
  Default = 0,
  Export = 1,
  Local = 2,
}

impl convert::TryFrom<u8> for Visibility {
  type Error = bebop::DecodeError;
  fn try_from(value: u8) -> result::Result<Self, bebop::DecodeError> {
    match value {
      0 => result::Result::Ok(Self::Default),
      1 => result::Result::Ok(Self::Export),
      2 => result::Result::Ok(Self::Local),
      _ => result::Result::Err(bebop::DecodeError::InvalidEnum {
        type_name: "Visibility",
        value: value as u64,
      }),
    }
  }
}

impl convert::From<Visibility> for u8 {
  fn from(value: Visibility) -> u8 {
    value as u8
  }
}

impl Visibility {
  pub const FIXED_ENCODED_SIZE: usize = 1;
  // @@bebop_insertion_point(enum_scope:Visibility)
}

impl bebop::BebopEncode for Visibility {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:Visibility)
    writer.write_byte(*self as u8);
    // @@bebop_insertion_point(encode_end:Visibility)
  }

  fn encoded_size(&self) -> usize {
    Self::FIXED_ENCODED_SIZE
  }
}

impl<'buf> bebop::BebopDecode<'buf> for Visibility {
  #[inline(always)]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:Visibility)
    let value = reader.read_byte()?;
    // @@bebop_insertion_point(decode_end:Visibility)
    convert::TryFrom::try_from(value)
  }
}

/// Literal value kinds.
/// Discriminates which value field of LiteralValue is set.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum LiteralKind {
  Unknown = 0,
  Bool = 1,
  Int = 2,
  Float = 3,
  String = 4,
  Uuid = 5,
  Bytes = 6,
  Timestamp = 7,
  Duration = 8,
}

impl convert::TryFrom<u8> for LiteralKind {
  type Error = bebop::DecodeError;
  fn try_from(value: u8) -> result::Result<Self, bebop::DecodeError> {
    match value {
      0 => result::Result::Ok(Self::Unknown),
      1 => result::Result::Ok(Self::Bool),
      2 => result::Result::Ok(Self::Int),
      3 => result::Result::Ok(Self::Float),
      4 => result::Result::Ok(Self::String),
      5 => result::Result::Ok(Self::Uuid),
      6 => result::Result::Ok(Self::Bytes),
      7 => result::Result::Ok(Self::Timestamp),
      8 => result::Result::Ok(Self::Duration),
      _ => result::Result::Err(bebop::DecodeError::InvalidEnum {
        type_name: "LiteralKind",
        value: value as u64,
      }),
    }
  }
}

impl convert::From<LiteralKind> for u8 {
  fn from(value: LiteralKind) -> u8 {
    value as u8
  }
}

impl LiteralKind {
  pub const FIXED_ENCODED_SIZE: usize = 1;
  // @@bebop_insertion_point(enum_scope:LiteralKind)
}

impl bebop::BebopEncode for LiteralKind {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:LiteralKind)
    writer.write_byte(*self as u8);
    // @@bebop_insertion_point(encode_end:LiteralKind)
  }

  fn encoded_size(&self) -> usize {
    Self::FIXED_ENCODED_SIZE
  }
}

impl<'buf> bebop::BebopDecode<'buf> for LiteralKind {
  #[inline(always)]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:LiteralKind)
    let value = reader.read_byte()?;
    // @@bebop_insertion_point(decode_end:LiteralKind)
    convert::TryFrom::try_from(value)
  }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct DecoratorTarget(pub u8);

#[allow(non_upper_case_globals)]
impl DecoratorTarget {
  pub const FIXED_ENCODED_SIZE: usize = 1;
  pub const NONE: Self = Self(0);
  pub const ENUM: Self = Self(1);
  pub const STRUCT: Self = Self(2);
  pub const MESSAGE: Self = Self(4);
  pub const UNION: Self = Self(8);
  pub const FIELD: Self = Self(16);
  pub const SERVICE: Self = Self(32);
  pub const METHOD: Self = Self(64);
  pub const BRANCH: Self = Self(128);
  pub const ALL: Self = Self(255);
  // @@bebop_insertion_point(enum_scope:DecoratorTarget)
}

impl bebop::BebopFlags for DecoratorTarget {
  type Bits = u8;
  const ALL_BITS: Self::Bits = 255;
  fn bits(self) -> Self::Bits {
    self.0
  }
  fn from_bits_retain(bits: Self::Bits) -> Self {
    Self(bits)
  }
}

impl ops::BitOr for DecoratorTarget {
  type Output = Self;
  fn bitor(self, rhs: Self) -> Self {
    Self(self.0 | rhs.0)
  }
}
impl ops::BitOrAssign for DecoratorTarget {
  fn bitor_assign(&mut self, rhs: Self) {
    self.0 |= rhs.0;
  }
}
impl ops::BitAnd for DecoratorTarget {
  type Output = Self;
  fn bitand(self, rhs: Self) -> Self {
    Self(self.0 & rhs.0)
  }
}
impl ops::BitAndAssign for DecoratorTarget {
  fn bitand_assign(&mut self, rhs: Self) {
    self.0 &= rhs.0;
  }
}
impl ops::BitXor for DecoratorTarget {
  type Output = Self;
  fn bitxor(self, rhs: Self) -> Self {
    Self(self.0 ^ rhs.0)
  }
}
impl ops::BitXorAssign for DecoratorTarget {
  fn bitxor_assign(&mut self, rhs: Self) {
    self.0 ^= rhs.0;
  }
}
impl ops::Not for DecoratorTarget {
  type Output = Self;
  fn not(self) -> Self {
    Self(!self.0)
  }
}
impl ops::Sub for DecoratorTarget {
  type Output = Self;
  fn sub(self, rhs: Self) -> Self {
    Self(self.0 & !rhs.0)
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DecoratorTarget {
  #[inline(always)]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    let bits = reader.read_byte()?;
    <Self as bebop::BebopFlags>::from_bits(bits).ok_or(bebop::DecodeError::InvalidFlags {
      type_name: "DecoratorTarget",
      bits: bits as u64,
    })
  }
}

/// Schema edition markers.
/// Edition values are ordered for comparison. Higher values are later editions.
/// A compiler rejects source files declaring an edition it does not support.
#[repr(i32)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Edition {
  Unknown = 0,
  Edition2026 = 1000,
  Max = 2147483647,
}

impl convert::TryFrom<i32> for Edition {
  type Error = bebop::DecodeError;
  fn try_from(value: i32) -> result::Result<Self, bebop::DecodeError> {
    match value {
      0 => result::Result::Ok(Self::Unknown),
      1000 => result::Result::Ok(Self::Edition2026),
      2147483647 => result::Result::Ok(Self::Max),
      _ => result::Result::Err(bebop::DecodeError::InvalidEnum {
        type_name: "Edition",
        value: value as u64,
      }),
    }
  }
}

impl convert::From<Edition> for i32 {
  fn from(value: Edition) -> i32 {
    value as i32
  }
}

impl Edition {
  pub const FIXED_ENCODED_SIZE: usize = 4;
  // @@bebop_insertion_point(enum_scope:Edition)
}

impl bebop::BebopEncode for Edition {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:Edition)
    writer.write_i32(*self as i32);
    // @@bebop_insertion_point(encode_end:Edition)
  }

  fn encoded_size(&self) -> usize {
    Self::FIXED_ENCODED_SIZE
  }
}

impl<'buf> bebop::BebopDecode<'buf> for Edition {
  #[inline(always)]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:Edition)
    let value = reader.read_i32()?;
    // @@bebop_insertion_point(decode_end:Edition)
    convert::TryFrom::try_from(value)
  }
}

/// Type reference.
/// Every field type, const type, method parameter, array element, map key,
/// and map value in a descriptor is a TypeDescriptor. The `kind` field
/// determines which other fields are present:
/// - Scalars (`BOOL`..`DURATION`): no additional fields
/// - `ARRAY`: `array_element` is the element type
/// - `FIXED_ARRAY`: `fixed_array_element` + `fixed_array_size`
/// - `MAP`: `map_key` + `map_value`
/// - `DEFINED`: `defined_fqn` names the referenced type
/// TypeDescriptor is recursive. An array of maps:
/// ```
/// kind=ARRAY
/// array_element.kind=MAP
/// array_element.map_key.kind=STRING
/// array_element.map_value.kind=DEFINED
/// array_element.map_value.defined_fqn="mypackage.Item"
/// ```
#[derive(Debug, Clone, Default, PartialEq, Eq, Hash)]
pub struct TypeDescriptor<'buf> {
  /// Discriminates which fields below are populated.
  pub kind: option::Option<TypeKind>,
  /// Element type when `kind == ARRAY`.
  pub array_element: option::Option<boxed::Box<TypeDescriptor<'buf>>>,
  /// Element type when `kind == FIXED_ARRAY`.
  pub fixed_array_element: option::Option<boxed::Box<TypeDescriptor<'buf>>>,
  /// Element count when `kind == FIXED_ARRAY`. Range 1-65535.
  pub fixed_array_size: option::Option<u32>,
  /// Key type when `kind == MAP`. Must be hashable (bool, integers, string, uuid).
  pub map_key: option::Option<boxed::Box<TypeDescriptor<'buf>>>,
  /// Value type when `kind == MAP`.
  pub map_value: option::Option<boxed::Box<TypeDescriptor<'buf>>>,
  /// FQN when `kind == DEFINED`. Always fully qualified after linking.
  pub defined_fqn: option::Option<borrow::Cow<'buf, str>>,
}

pub type TypeDescriptorOwned = TypeDescriptor<'static>;

impl<'buf> TypeDescriptor<'buf> {
  pub fn into_owned(self) -> TypeDescriptorOwned {
    TypeDescriptor {
      kind: self.kind,
      array_element: self.array_element.map(|v| boxed::Box::new(v.into_owned())),
      fixed_array_element: self
        .fixed_array_element
        .map(|v| boxed::Box::new(v.into_owned())),
      fixed_array_size: self.fixed_array_size,
      map_key: self.map_key.map(|v| boxed::Box::new(v.into_owned())),
      map_value: self.map_value.map(|v| boxed::Box::new(v.into_owned())),
      defined_fqn: self.defined_fqn.map(|v| borrow::Cow::Owned(v.into_owned())),
    }
  }
}

impl<'buf> bebop::BebopEncode for TypeDescriptor<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:TypeDescriptor)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.kind {
      writer.write_tag(1);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.array_element {
      writer.write_tag(2);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.fixed_array_element {
      writer.write_tag(3);
      v.encode(writer);
    }
    if let option::Option::Some(v) = self.fixed_array_size {
      writer.write_tag(4);
      writer.write_u32(v);
    }
    if let option::Option::Some(ref v) = self.map_key {
      writer.write_tag(5);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.map_value {
      writer.write_tag(6);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.defined_fqn {
      writer.write_tag(7);
      writer.write_string(&v);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:TypeDescriptor)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.kind {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.array_element {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.fixed_array_element {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(v) = self.fixed_array_size {
      size += bebop::wire_size::tagged_size(mem::size_of::<u32>());
    }
    if let option::Option::Some(ref v) = self.map_key {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.map_value {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.defined_fqn {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for TypeDescriptor<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:TypeDescriptor)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.kind =
            option::Option::Some(TypeKind::decode(reader).for_field("TypeDescriptor", "kind")?)
        }
        2 => {
          msg.array_element = option::Option::Some(boxed::Box::new(
            TypeDescriptor::decode(reader).for_field("TypeDescriptor", "array_element")?,
          ))
        }
        3 => {
          msg.fixed_array_element = option::Option::Some(boxed::Box::new(
            TypeDescriptor::decode(reader).for_field("TypeDescriptor", "fixed_array_element")?,
          ))
        }
        4 => {
          msg.fixed_array_size = option::Option::Some(
            reader
              .read_u32()
              .for_field("TypeDescriptor", "fixed_array_size")?,
          )
        }
        5 => {
          msg.map_key = option::Option::Some(boxed::Box::new(
            TypeDescriptor::decode(reader).for_field("TypeDescriptor", "map_key")?,
          ))
        }
        6 => {
          msg.map_value = option::Option::Some(boxed::Box::new(
            TypeDescriptor::decode(reader).for_field("TypeDescriptor", "map_value")?,
          ))
        }
        7 => {
          msg.defined_fqn = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("TypeDescriptor", "defined_fqn")?,
          ))
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "TypeDescriptor",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:TypeDescriptor)
    result::Result::Ok(msg)
  }
}

impl<'buf> TypeDescriptor<'buf> {
  pub fn with_kind(mut self, value: TypeKind) -> Self {
    self.kind = option::Option::Some(value);
    self
  }
  pub fn with_array_element(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.array_element = option::Option::Some(boxed::Box::new(value));
    self
  }
  pub fn with_fixed_array_element(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.fixed_array_element = option::Option::Some(boxed::Box::new(value));
    self
  }
  pub fn with_fixed_array_size(mut self, value: u32) -> Self {
    self.fixed_array_size = option::Option::Some(value);
    self
  }
  pub fn with_map_key(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.map_key = option::Option::Some(boxed::Box::new(value));
    self
  }
  pub fn with_map_value(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.map_value = option::Option::Some(boxed::Box::new(value));
    self
  }
  pub fn with_defined_fqn(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.defined_fqn = option::Option::Some(value.into());
    self
  }
  // @@bebop_insertion_point(message_scope:TypeDescriptor)
}

/// Concrete value.
/// Used in const definitions, decorator arguments, and decorator parameter
/// defaults/constraints. One typed value field is set, determined by `kind`.
/// The `raw_value` field preserves pre-expansion text for constants using
/// environment variable substitution (`$(...)`).
#[derive(Debug, Clone, Default, PartialEq)]
pub struct LiteralValue<'buf> {
  pub kind: option::Option<LiteralKind>,
  pub bool_value: option::Option<bool>,
  pub int_value: option::Option<i64>,
  pub float_value: option::Option<f64>,
  pub string_value: option::Option<borrow::Cow<'buf, str>>,
  pub uuid_value: option::Option<bebop::Uuid>,
  /// Original source text before `$(...)` expansion. Only set for string
  /// literals that contained environment variable references.
  pub raw_value: option::Option<borrow::Cow<'buf, str>>,
  /// When `kind == BYTES`.
  pub bytes_value: option::Option<bebop::BebopBytes<'buf>>,
  /// When `kind == TIMESTAMP`.
  pub timestamp_value: option::Option<bebop::BebopTimestamp>,
  /// When `kind == DURATION`.
  pub duration_value: option::Option<bebop::BebopDuration>,
}

pub type LiteralValueOwned = LiteralValue<'static>;

impl<'buf> LiteralValue<'buf> {
  pub fn into_owned(self) -> LiteralValueOwned {
    LiteralValue {
      kind: self.kind,
      bool_value: self.bool_value,
      int_value: self.int_value,
      float_value: self.float_value,
      string_value: self
        .string_value
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      uuid_value: self.uuid_value,
      raw_value: self.raw_value.map(|v| borrow::Cow::Owned(v.into_owned())),
      bytes_value: self.bytes_value.map(|v| v.into_owned()),
      timestamp_value: self.timestamp_value,
      duration_value: self.duration_value,
    }
  }
}

impl<'buf> bebop::BebopEncode for LiteralValue<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:LiteralValue)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.kind {
      writer.write_tag(1);
      v.encode(writer);
    }
    if let option::Option::Some(v) = self.bool_value {
      writer.write_tag(2);
      writer.write_bool(v);
    }
    if let option::Option::Some(v) = self.int_value {
      writer.write_tag(3);
      writer.write_i64(v);
    }
    if let option::Option::Some(v) = self.float_value {
      writer.write_tag(4);
      writer.write_f64(v);
    }
    if let option::Option::Some(ref v) = self.string_value {
      writer.write_tag(5);
      writer.write_string(&v);
    }
    if let option::Option::Some(v) = self.uuid_value {
      writer.write_tag(6);
      writer.write_uuid(v);
    }
    if let option::Option::Some(ref v) = self.raw_value {
      writer.write_tag(7);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.bytes_value {
      writer.write_tag(8);
      writer.write_byte_array(&v);
    }
    if let option::Option::Some(v) = self.timestamp_value {
      writer.write_tag(9);
      writer.write_timestamp(v);
    }
    if let option::Option::Some(v) = self.duration_value {
      writer.write_tag(10);
      writer.write_duration(v);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:LiteralValue)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.kind {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(v) = self.bool_value {
      size += bebop::wire_size::tagged_size(mem::size_of::<bool>());
    }
    if let option::Option::Some(v) = self.int_value {
      size += bebop::wire_size::tagged_size(mem::size_of::<i64>());
    }
    if let option::Option::Some(v) = self.float_value {
      size += bebop::wire_size::tagged_size(mem::size_of::<f64>());
    }
    if let option::Option::Some(ref v) = self.string_value {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(v) = self.uuid_value {
      size += bebop::wire_size::tagged_size(mem::size_of::<bebop::Uuid>());
    }
    if let option::Option::Some(ref v) = self.raw_value {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.bytes_value {
      size += bebop::wire_size::tagged_size(bebop::wire_size::byte_array_size(v.len()));
    }
    if let option::Option::Some(v) = self.timestamp_value {
      size += bebop::wire_size::tagged_size(mem::size_of::<i64>() + mem::size_of::<i32>());
    }
    if let option::Option::Some(v) = self.duration_value {
      size += bebop::wire_size::tagged_size(mem::size_of::<i64>() + mem::size_of::<i32>());
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for LiteralValue<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:LiteralValue)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.kind =
            option::Option::Some(LiteralKind::decode(reader).for_field("LiteralValue", "kind")?)
        }
        2 => {
          msg.bool_value =
            option::Option::Some(reader.read_bool().for_field("LiteralValue", "bool_value")?)
        }
        3 => {
          msg.int_value =
            option::Option::Some(reader.read_i64().for_field("LiteralValue", "int_value")?)
        }
        4 => {
          msg.float_value =
            option::Option::Some(reader.read_f64().for_field("LiteralValue", "float_value")?)
        }
        5 => {
          msg.string_value = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("LiteralValue", "string_value")?,
          ))
        }
        6 => {
          msg.uuid_value =
            option::Option::Some(reader.read_uuid().for_field("LiteralValue", "uuid_value")?)
        }
        7 => {
          msg.raw_value = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("LiteralValue", "raw_value")?,
          ))
        }
        8 => {
          msg.bytes_value = option::Option::Some(bebop::BebopBytes::borrowed(
            reader
              .read_byte_slice()
              .for_field("LiteralValue", "bytes_value")?,
          ))
        }
        9 => {
          msg.timestamp_value = option::Option::Some(
            reader
              .read_timestamp()
              .for_field("LiteralValue", "timestamp_value")?,
          )
        }
        10 => {
          msg.duration_value = option::Option::Some(
            reader
              .read_duration()
              .for_field("LiteralValue", "duration_value")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "LiteralValue",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:LiteralValue)
    result::Result::Ok(msg)
  }
}

impl<'buf> LiteralValue<'buf> {
  pub fn with_kind(mut self, value: LiteralKind) -> Self {
    self.kind = option::Option::Some(value);
    self
  }
  pub fn with_bool_value(mut self, value: bool) -> Self {
    self.bool_value = option::Option::Some(value);
    self
  }
  pub fn with_int_value(mut self, value: i64) -> Self {
    self.int_value = option::Option::Some(value);
    self
  }
  pub fn with_float_value(mut self, value: f64) -> Self {
    self.float_value = option::Option::Some(value);
    self
  }
  pub fn with_string_value(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.string_value = option::Option::Some(value.into());
    self
  }
  pub fn with_uuid_value(mut self, value: bebop::Uuid) -> Self {
    self.uuid_value = option::Option::Some(value);
    self
  }
  pub fn with_raw_value(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.raw_value = option::Option::Some(value.into());
    self
  }
  pub fn with_bytes_value(mut self, value: impl convert::Into<bebop::BebopBytes<'buf>>) -> Self {
    self.bytes_value = option::Option::Some(value.into());
    self
  }
  pub fn with_timestamp_value(mut self, value: bebop::BebopTimestamp) -> Self {
    self.timestamp_value = option::Option::Some(value);
    self
  }
  pub fn with_duration_value(mut self, value: bebop::BebopDuration) -> Self {
    self.duration_value = option::Option::Some(value);
    self
  }
  // @@bebop_insertion_point(message_scope:LiteralValue)
}

/// Named or positional argument in a decorator usage.
/// Positional arguments have an empty string for `name`.
#[derive(Debug, Clone, PartialEq)]
pub struct DecoratorArg<'buf> {
  pub name: borrow::Cow<'buf, str>,
  pub value: LiteralValue<'buf>,
}

pub type DecoratorArgOwned = DecoratorArg<'static>;

impl<'buf> DecoratorArg<'buf> {
  pub fn new(
    name: impl convert::Into<borrow::Cow<'buf, str>>,
    value: LiteralValue<'static>,
  ) -> Self {
    let name = name.into();
    Self { name, value }
  }
}

impl<'buf> DecoratorArg<'buf> {
  pub fn into_owned(self) -> DecoratorArgOwned {
    DecoratorArg {
      name: borrow::Cow::Owned(self.name.into_owned()),
      value: self.value.into_owned(),
    }
  }
}

impl<'buf> bebop::BebopEncode for DecoratorArg<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:DecoratorArg)
    writer.write_string(&self.name);
    self.value.encode(writer);
    // @@bebop_insertion_point(encode_end:DecoratorArg)
  }

  fn encoded_size(&self) -> usize {
    let mut size = 0;
    size += bebop::wire_size::string_size(self.name.len());
    size += self.value.encoded_size();
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DecoratorArg<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:DecoratorArg)
    let name = borrow::Cow::Borrowed(reader.read_str().for_field("DecoratorArg", "name")?);
    let value = LiteralValue::decode(reader).for_field("DecoratorArg", "value")?;
    // @@bebop_insertion_point(decode_end:DecoratorArg)
    result::Result::Ok(DecoratorArg { name, value })
  }
}

impl<'buf> DecoratorArg<'buf> {
  // @@bebop_insertion_point(struct_scope:DecoratorArg)
}

/// Decorator applied to a definition, field, enum member, branch, or method.
/// The `fqn` identifies which decorator definition this usage corresponds to.
/// Always fully qualified after linking.
/// The `export_data` map holds key-value pairs from executing the decorator's
/// Lua export block. Generators read these to access decorator-computed
/// metadata without re-running Lua.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct DecoratorUsage<'buf> {
  /// FQN of the decorator definition (e.g., `validators.range`).
  pub fqn: option::Option<borrow::Cow<'buf, str>>,
  /// Arguments passed at the usage site, in declaration order.
  pub args: option::Option<vec::Vec<DecoratorArg<'buf>>>,
  /// Results from the decorator's export block.
  pub export_data: option::Option<bebop::HashMap<borrow::Cow<'buf, str>, LiteralValue<'buf>>>,
}

pub type DecoratorUsageOwned = DecoratorUsage<'static>;

impl<'buf> DecoratorUsage<'buf> {
  pub fn into_owned(self) -> DecoratorUsageOwned {
    DecoratorUsage {
      fqn: self.fqn.map(|v| borrow::Cow::Owned(v.into_owned())),
      args: self
        .args
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      export_data: self.export_data.map(|v| {
        v.into_iter()
          .map(|(_k, _v)| (borrow::Cow::Owned(_k.into_owned()), _v.into_owned()))
          .collect()
      }),
    }
  }
}

impl<'buf> bebop::BebopEncode for DecoratorUsage<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:DecoratorUsage)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.fqn {
      writer.write_tag(1);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.args {
      writer.write_tag(2);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(ref v) = self.export_data {
      writer.write_tag(3);
      writer.write_map(&v, |_w, _k, _v| {
        _w.write_string(&_k);
        _v.encode(_w);
      });
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:DecoratorUsage)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.fqn {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.args {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(ref v) = self.export_data {
      size += bebop::wire_size::tagged_size(bebop::wire_size::map_size(v, |_k, _v| {
        bebop::wire_size::string_size(_k.len()) + _v.encoded_size()
      }));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DecoratorUsage<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:DecoratorUsage)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.fqn = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("DecoratorUsage", "fqn")?,
          ))
        }
        2 => {
          msg.args = option::Option::Some(
            reader
              .read_array(|_r| DecoratorArg::decode(_r))
              .for_field("DecoratorUsage", "args")?,
          )
        }
        3 => {
          msg.export_data = option::Option::Some(
            reader
              .read_map(|_r| {
                result::Result::Ok((
                  result::Result::Ok(borrow::Cow::Borrowed(_r.read_str()?))?,
                  LiteralValue::decode(_r)?,
                ))
              })
              .for_field("DecoratorUsage", "export_data")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "DecoratorUsage",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:DecoratorUsage)
    result::Result::Ok(msg)
  }
}

impl<'buf> DecoratorUsage<'buf> {
  pub fn with_fqn(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.fqn = option::Option::Some(value.into());
    self
  }
  pub fn with_args(mut self, value: impl iter::IntoIterator<Item = DecoratorArg<'buf>>) -> Self {
    self.args = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_export_data(
    mut self,
    value: impl iter::IntoIterator<
      Item = (
        impl convert::Into<borrow::Cow<'buf, str>>,
        LiteralValue<'buf>,
      ),
    >,
  ) -> Self {
    self.export_data =
      option::Option::Some(value.into_iter().map(|(_k, _v)| (_k.into(), _v)).collect());
    self
  }
  // @@bebop_insertion_point(message_scope:DecoratorUsage)
}

/// Field in a struct or message.
/// Struct and message fields share this type, distinguished by `index`:
/// - Struct fields: `index == 0` (positional encoding, no tag byte)
/// - Message fields: `index` 1-255 (wire tag from source)
/// Struct fields encode in declaration order with no separators. Message
/// fields encode as (tag, value) pairs in any order, terminated by 0.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct FieldDescriptor<'buf> {
  pub name: option::Option<borrow::Cow<'buf, str>>,
  /// Text from preceding `///` comments in source.
  pub documentation: option::Option<borrow::Cow<'buf, str>>,
  pub r#type: option::Option<TypeDescriptor<'buf>>,
  /// Wire tag: 0 for struct fields, 1-255 for message fields.
  pub index: option::Option<u32>,
  pub decorators: option::Option<vec::Vec<DecoratorUsage<'buf>>>,
}

pub type FieldDescriptorOwned = FieldDescriptor<'static>;

impl<'buf> FieldDescriptor<'buf> {
  pub fn into_owned(self) -> FieldDescriptorOwned {
    FieldDescriptor {
      name: self.name.map(|v| borrow::Cow::Owned(v.into_owned())),
      documentation: self
        .documentation
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      r#type: self.r#type.map(|v| v.into_owned()),
      index: self.index,
      decorators: self
        .decorators
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for FieldDescriptor<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:FieldDescriptor)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.name {
      writer.write_tag(1);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.documentation {
      writer.write_tag(2);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.r#type {
      writer.write_tag(3);
      v.encode(writer);
    }
    if let option::Option::Some(v) = self.index {
      writer.write_tag(4);
      writer.write_u32(v);
    }
    if let option::Option::Some(ref v) = self.decorators {
      writer.write_tag(5);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:FieldDescriptor)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.name {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.documentation {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.r#type {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(v) = self.index {
      size += bebop::wire_size::tagged_size(mem::size_of::<u32>());
    }
    if let option::Option::Some(ref v) = self.decorators {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for FieldDescriptor<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:FieldDescriptor)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.name = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("FieldDescriptor", "name")?,
          ))
        }
        2 => {
          msg.documentation = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("FieldDescriptor", "documentation")?,
          ))
        }
        3 => {
          msg.r#type = option::Option::Some(
            TypeDescriptor::decode(reader).for_field("FieldDescriptor", "type")?,
          )
        }
        4 => {
          msg.index = option::Option::Some(reader.read_u32().for_field("FieldDescriptor", "index")?)
        }
        5 => {
          msg.decorators = option::Option::Some(
            reader
              .read_array(|_r| DecoratorUsage::decode(_r))
              .for_field("FieldDescriptor", "decorators")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "FieldDescriptor",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:FieldDescriptor)
    result::Result::Ok(msg)
  }
}

impl<'buf> FieldDescriptor<'buf> {
  pub fn with_name(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.name = option::Option::Some(value.into());
    self
  }
  pub fn with_documentation(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.documentation = option::Option::Some(value.into());
    self
  }
  pub fn with_type(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.r#type = option::Option::Some(value);
    self
  }
  pub fn with_index(mut self, value: u32) -> Self {
    self.index = option::Option::Some(value);
    self
  }
  pub fn with_decorators(
    mut self,
    value: impl iter::IntoIterator<Item = DecoratorUsage<'buf>>,
  ) -> Self {
    self.decorators = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:FieldDescriptor)
}

/// Enum member (name + integer value).
/// The `value` is stored as uint64 regardless of the enum's base type.
/// For signed base types, sign-extend from the base type's bit width.
/// A member with value 0xFFFFFFFFFFFFFFFF in an int8-based enum is -1.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct EnumMemberDescriptor<'buf> {
  pub name: option::Option<borrow::Cow<'buf, str>>,
  pub documentation: option::Option<borrow::Cow<'buf, str>>,
  /// Stored unsigned. Reinterpret per the parent enum's `base_type`.
  pub value: option::Option<u64>,
  pub decorators: option::Option<vec::Vec<DecoratorUsage<'buf>>>,
  /// Original expression text (e.g., `1 << 3`). Absent for simple literals.
  pub value_expr: option::Option<borrow::Cow<'buf, str>>,
}

pub type EnumMemberDescriptorOwned = EnumMemberDescriptor<'static>;

impl<'buf> EnumMemberDescriptor<'buf> {
  pub fn into_owned(self) -> EnumMemberDescriptorOwned {
    EnumMemberDescriptor {
      name: self.name.map(|v| borrow::Cow::Owned(v.into_owned())),
      documentation: self
        .documentation
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      value: self.value,
      decorators: self
        .decorators
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      value_expr: self.value_expr.map(|v| borrow::Cow::Owned(v.into_owned())),
    }
  }
}

impl<'buf> bebop::BebopEncode for EnumMemberDescriptor<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:EnumMemberDescriptor)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.name {
      writer.write_tag(1);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.documentation {
      writer.write_tag(2);
      writer.write_string(&v);
    }
    if let option::Option::Some(v) = self.value {
      writer.write_tag(3);
      writer.write_u64(v);
    }
    if let option::Option::Some(ref v) = self.decorators {
      writer.write_tag(4);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(ref v) = self.value_expr {
      writer.write_tag(5);
      writer.write_string(&v);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:EnumMemberDescriptor)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.name {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.documentation {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(v) = self.value {
      size += bebop::wire_size::tagged_size(mem::size_of::<u64>());
    }
    if let option::Option::Some(ref v) = self.decorators {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(ref v) = self.value_expr {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for EnumMemberDescriptor<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:EnumMemberDescriptor)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.name = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("EnumMemberDescriptor", "name")?,
          ))
        }
        2 => {
          msg.documentation = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("EnumMemberDescriptor", "documentation")?,
          ))
        }
        3 => {
          msg.value = option::Option::Some(
            reader
              .read_u64()
              .for_field("EnumMemberDescriptor", "value")?,
          )
        }
        4 => {
          msg.decorators = option::Option::Some(
            reader
              .read_array(|_r| DecoratorUsage::decode(_r))
              .for_field("EnumMemberDescriptor", "decorators")?,
          )
        }
        5 => {
          msg.value_expr = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("EnumMemberDescriptor", "value_expr")?,
          ))
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "EnumMemberDescriptor",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:EnumMemberDescriptor)
    result::Result::Ok(msg)
  }
}

impl<'buf> EnumMemberDescriptor<'buf> {
  pub fn with_name(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.name = option::Option::Some(value.into());
    self
  }
  pub fn with_documentation(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.documentation = option::Option::Some(value.into());
    self
  }
  pub fn with_value(mut self, value: u64) -> Self {
    self.value = option::Option::Some(value);
    self
  }
  pub fn with_decorators(
    mut self,
    value: impl iter::IntoIterator<Item = DecoratorUsage<'buf>>,
  ) -> Self {
    self.decorators = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_value_expr(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.value_expr = option::Option::Some(value.into());
    self
  }
  // @@bebop_insertion_point(message_scope:EnumMemberDescriptor)
}

/// Union branch.
/// Each branch has a discriminator byte (1-255) written before the payload.
/// Two modes:
/// **Inline branch**: body declared in the union.
/// ```bebop
/// union Shape { Circle(1): { radius: float32; } }
/// ```
/// Sets `inline_fqn = "mypackage.Shape.Circle"`. The inline definition
/// lives in the parent's `nested` array.
/// **Type-reference branch**: references an existing type.
/// ```bebop
/// union Shape { rect(2): Rect }
/// ```
/// Sets `type_ref_fqn = "mypackage.Rect"` and `name = "rect"`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct UnionBranchDescriptor<'buf> {
  /// Wire discriminator byte. Range 1-255.
  pub discriminator: option::Option<u8>,
  pub documentation: option::Option<borrow::Cow<'buf, str>>,
  /// FQN of inline definition. Mutually exclusive with `type_ref_fqn`.
  pub inline_fqn: option::Option<borrow::Cow<'buf, str>>,
  /// FQN of referenced type. Mutually exclusive with `inline_fqn`.
  pub type_ref_fqn: option::Option<borrow::Cow<'buf, str>>,
  /// Branch name for type-reference branches.
  pub name: option::Option<borrow::Cow<'buf, str>>,
  pub decorators: option::Option<vec::Vec<DecoratorUsage<'buf>>>,
}

pub type UnionBranchDescriptorOwned = UnionBranchDescriptor<'static>;

impl<'buf> UnionBranchDescriptor<'buf> {
  pub fn into_owned(self) -> UnionBranchDescriptorOwned {
    UnionBranchDescriptor {
      discriminator: self.discriminator,
      documentation: self
        .documentation
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      inline_fqn: self.inline_fqn.map(|v| borrow::Cow::Owned(v.into_owned())),
      type_ref_fqn: self
        .type_ref_fqn
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      name: self.name.map(|v| borrow::Cow::Owned(v.into_owned())),
      decorators: self
        .decorators
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for UnionBranchDescriptor<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:UnionBranchDescriptor)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(v) = self.discriminator {
      writer.write_tag(1);
      writer.write_byte(v);
    }
    if let option::Option::Some(ref v) = self.documentation {
      writer.write_tag(2);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.inline_fqn {
      writer.write_tag(3);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.type_ref_fqn {
      writer.write_tag(4);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.name {
      writer.write_tag(5);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.decorators {
      writer.write_tag(6);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:UnionBranchDescriptor)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(v) = self.discriminator {
      size += bebop::wire_size::tagged_size(mem::size_of::<u8>());
    }
    if let option::Option::Some(ref v) = self.documentation {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.inline_fqn {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.type_ref_fqn {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.name {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.decorators {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for UnionBranchDescriptor<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:UnionBranchDescriptor)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.discriminator = option::Option::Some(
            reader
              .read_byte()
              .for_field("UnionBranchDescriptor", "discriminator")?,
          )
        }
        2 => {
          msg.documentation = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("UnionBranchDescriptor", "documentation")?,
          ))
        }
        3 => {
          msg.inline_fqn = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("UnionBranchDescriptor", "inline_fqn")?,
          ))
        }
        4 => {
          msg.type_ref_fqn = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("UnionBranchDescriptor", "type_ref_fqn")?,
          ))
        }
        5 => {
          msg.name = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("UnionBranchDescriptor", "name")?,
          ))
        }
        6 => {
          msg.decorators = option::Option::Some(
            reader
              .read_array(|_r| DecoratorUsage::decode(_r))
              .for_field("UnionBranchDescriptor", "decorators")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "UnionBranchDescriptor",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:UnionBranchDescriptor)
    result::Result::Ok(msg)
  }
}

impl<'buf> UnionBranchDescriptor<'buf> {
  pub fn with_discriminator(mut self, value: u8) -> Self {
    self.discriminator = option::Option::Some(value);
    self
  }
  pub fn with_documentation(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.documentation = option::Option::Some(value.into());
    self
  }
  pub fn with_inline_fqn(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.inline_fqn = option::Option::Some(value.into());
    self
  }
  pub fn with_type_ref_fqn(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.type_ref_fqn = option::Option::Some(value.into());
    self
  }
  pub fn with_name(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.name = option::Option::Some(value.into());
    self
  }
  pub fn with_decorators(
    mut self,
    value: impl iter::IntoIterator<Item = DecoratorUsage<'buf>>,
  ) -> Self {
    self.decorators = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:UnionBranchDescriptor)
}

/// Service method.
/// The `id` is MurmurHash3 of `/ServiceName/MethodName`, computed at compile
/// time. Gives a stable 32-bit routing key without transmitting the full name.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct MethodDescriptor<'buf> {
  pub name: option::Option<borrow::Cow<'buf, str>>,
  pub documentation: option::Option<borrow::Cow<'buf, str>>,
  pub request_type: option::Option<TypeDescriptor<'buf>>,
  pub response_type: option::Option<TypeDescriptor<'buf>>,
  pub method_type: option::Option<MethodType>,
  /// MurmurHash3 of `/ServiceName/MethodName`.
  pub id: option::Option<u32>,
  pub decorators: option::Option<vec::Vec<DecoratorUsage<'buf>>>,
}

pub type MethodDescriptorOwned = MethodDescriptor<'static>;

impl<'buf> MethodDescriptor<'buf> {
  pub fn into_owned(self) -> MethodDescriptorOwned {
    MethodDescriptor {
      name: self.name.map(|v| borrow::Cow::Owned(v.into_owned())),
      documentation: self
        .documentation
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      request_type: self.request_type.map(|v| v.into_owned()),
      response_type: self.response_type.map(|v| v.into_owned()),
      method_type: self.method_type,
      id: self.id,
      decorators: self
        .decorators
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for MethodDescriptor<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:MethodDescriptor)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.name {
      writer.write_tag(1);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.documentation {
      writer.write_tag(2);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.request_type {
      writer.write_tag(3);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.response_type {
      writer.write_tag(4);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.method_type {
      writer.write_tag(5);
      v.encode(writer);
    }
    if let option::Option::Some(v) = self.id {
      writer.write_tag(6);
      writer.write_u32(v);
    }
    if let option::Option::Some(ref v) = self.decorators {
      writer.write_tag(7);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:MethodDescriptor)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.name {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.documentation {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.request_type {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.response_type {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.method_type {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(v) = self.id {
      size += bebop::wire_size::tagged_size(mem::size_of::<u32>());
    }
    if let option::Option::Some(ref v) = self.decorators {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for MethodDescriptor<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:MethodDescriptor)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.name = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("MethodDescriptor", "name")?,
          ))
        }
        2 => {
          msg.documentation = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("MethodDescriptor", "documentation")?,
          ))
        }
        3 => {
          msg.request_type = option::Option::Some(
            TypeDescriptor::decode(reader).for_field("MethodDescriptor", "request_type")?,
          )
        }
        4 => {
          msg.response_type = option::Option::Some(
            TypeDescriptor::decode(reader).for_field("MethodDescriptor", "response_type")?,
          )
        }
        5 => {
          msg.method_type = option::Option::Some(
            MethodType::decode(reader).for_field("MethodDescriptor", "method_type")?,
          )
        }
        6 => msg.id = option::Option::Some(reader.read_u32().for_field("MethodDescriptor", "id")?),
        7 => {
          msg.decorators = option::Option::Some(
            reader
              .read_array(|_r| DecoratorUsage::decode(_r))
              .for_field("MethodDescriptor", "decorators")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "MethodDescriptor",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:MethodDescriptor)
    result::Result::Ok(msg)
  }
}

impl<'buf> MethodDescriptor<'buf> {
  pub fn with_name(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.name = option::Option::Some(value.into());
    self
  }
  pub fn with_documentation(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.documentation = option::Option::Some(value.into());
    self
  }
  pub fn with_request_type(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.request_type = option::Option::Some(value);
    self
  }
  pub fn with_response_type(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.response_type = option::Option::Some(value);
    self
  }
  pub fn with_method_type(mut self, value: MethodType) -> Self {
    self.method_type = option::Option::Some(value);
    self
  }
  pub fn with_id(mut self, value: u32) -> Self {
    self.id = option::Option::Some(value);
    self
  }
  pub fn with_decorators(
    mut self,
    value: impl iter::IntoIterator<Item = DecoratorUsage<'buf>>,
  ) -> Self {
    self.decorators = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:MethodDescriptor)
}

/// Enum definition body.
/// The `base_type` indicates the declared integer width (e.g., `: uint8`).
/// Defaults to `UINT32` when no suffix is specified. Valid base types:
/// `BYTE`, `INT8`, `INT16`, `UINT16`, `INT32`, `UINT32`, `INT64`, `UINT64`.
/// Member values are stored as uint64 and reinterpreted per `base_type`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct EnumDef<'buf> {
  pub base_type: option::Option<TypeKind>,
  pub members: option::Option<vec::Vec<EnumMemberDescriptor<'buf>>>,
  /// True when `@flags` is applied. Members are bit positions for OR.
  pub is_flags: option::Option<bool>,
}

pub type EnumDefOwned = EnumDef<'static>;

impl<'buf> EnumDef<'buf> {
  pub fn into_owned(self) -> EnumDefOwned {
    EnumDef {
      base_type: self.base_type,
      members: self
        .members
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      is_flags: self.is_flags,
    }
  }
}

impl<'buf> bebop::BebopEncode for EnumDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:EnumDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.base_type {
      writer.write_tag(1);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.members {
      writer.write_tag(2);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(v) = self.is_flags {
      writer.write_tag(3);
      writer.write_bool(v);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:EnumDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.base_type {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.members {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(v) = self.is_flags {
      size += bebop::wire_size::tagged_size(mem::size_of::<bool>());
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for EnumDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:EnumDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.base_type =
            option::Option::Some(TypeKind::decode(reader).for_field("EnumDef", "base_type")?)
        }
        2 => {
          msg.members = option::Option::Some(
            reader
              .read_array(|_r| EnumMemberDescriptor::decode(_r))
              .for_field("EnumDef", "members")?,
          )
        }
        3 => {
          msg.is_flags = option::Option::Some(reader.read_bool().for_field("EnumDef", "is_flags")?)
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "EnumDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:EnumDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> EnumDef<'buf> {
  pub fn with_base_type(mut self, value: TypeKind) -> Self {
    self.base_type = option::Option::Some(value);
    self
  }
  pub fn with_members(
    mut self,
    value: impl iter::IntoIterator<Item = EnumMemberDescriptor<'buf>>,
  ) -> Self {
    self.members = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_is_flags(mut self, value: bool) -> Self {
    self.is_flags = option::Option::Some(value);
    self
  }
  // @@bebop_insertion_point(message_scope:EnumDef)
}

/// Struct definition body.
/// Fields encode positionally: bytes concatenated in declaration order,
/// no tags, no length prefix (for fixed-size structs). Variable-size structs
/// get a u32 byte-length prefix.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct StructDef<'buf> {
  pub fields: option::Option<vec::Vec<FieldDescriptor<'buf>>>,
  /// True when declared with `mut`. Mutable structs allow field reassignment.
  pub is_mutable: option::Option<bool>,
  /// Total wire bytes when all fields are fixed-size. Zero when any field
  /// is variable-size. Generators use this to pre-allocate buffers.
  pub fixed_size: option::Option<u32>,
}

pub type StructDefOwned = StructDef<'static>;

impl<'buf> StructDef<'buf> {
  pub fn into_owned(self) -> StructDefOwned {
    StructDef {
      fields: self
        .fields
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      is_mutable: self.is_mutable,
      fixed_size: self.fixed_size,
    }
  }
}

impl<'buf> bebop::BebopEncode for StructDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:StructDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.fields {
      writer.write_tag(1);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(v) = self.is_mutable {
      writer.write_tag(2);
      writer.write_bool(v);
    }
    if let option::Option::Some(v) = self.fixed_size {
      writer.write_tag(3);
      writer.write_u32(v);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:StructDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.fields {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(v) = self.is_mutable {
      size += bebop::wire_size::tagged_size(mem::size_of::<bool>());
    }
    if let option::Option::Some(v) = self.fixed_size {
      size += bebop::wire_size::tagged_size(mem::size_of::<u32>());
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for StructDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:StructDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.fields = option::Option::Some(
            reader
              .read_array(|_r| FieldDescriptor::decode(_r))
              .for_field("StructDef", "fields")?,
          )
        }
        2 => {
          msg.is_mutable =
            option::Option::Some(reader.read_bool().for_field("StructDef", "is_mutable")?)
        }
        3 => {
          msg.fixed_size =
            option::Option::Some(reader.read_u32().for_field("StructDef", "fixed_size")?)
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "StructDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:StructDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> StructDef<'buf> {
  pub fn with_fields(
    mut self,
    value: impl iter::IntoIterator<Item = FieldDescriptor<'buf>>,
  ) -> Self {
    self.fields = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_is_mutable(mut self, value: bool) -> Self {
    self.is_mutable = option::Option::Some(value);
    self
  }
  pub fn with_fixed_size(mut self, value: u32) -> Self {
    self.fixed_size = option::Option::Some(value);
    self
  }
  // @@bebop_insertion_point(message_scope:StructDef)
}

/// Message definition body.
/// Fields encode as tagged pairs (index, value), terminated by zero byte.
/// Fields can be added or removed without breaking existing decoders.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct MessageDef<'buf> {
  pub fields: option::Option<vec::Vec<FieldDescriptor<'buf>>>,
}

pub type MessageDefOwned = MessageDef<'static>;

impl<'buf> MessageDef<'buf> {
  pub fn into_owned(self) -> MessageDefOwned {
    MessageDef {
      fields: self
        .fields
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for MessageDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:MessageDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.fields {
      writer.write_tag(1);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:MessageDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.fields {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for MessageDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:MessageDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.fields = option::Option::Some(
            reader
              .read_array(|_r| FieldDescriptor::decode(_r))
              .for_field("MessageDef", "fields")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "MessageDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:MessageDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> MessageDef<'buf> {
  pub fn with_fields(
    mut self,
    value: impl iter::IntoIterator<Item = FieldDescriptor<'buf>>,
  ) -> Self {
    self.fields = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:MessageDef)
}

/// Union definition body.
/// Wire encoding: u32 byte_length, discriminator byte, branch payload.
/// Decoders read the length to skip unknown discriminators.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct UnionDef<'buf> {
  pub branches: option::Option<vec::Vec<UnionBranchDescriptor<'buf>>>,
}

pub type UnionDefOwned = UnionDef<'static>;

impl<'buf> UnionDef<'buf> {
  pub fn into_owned(self) -> UnionDefOwned {
    UnionDef {
      branches: self
        .branches
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for UnionDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:UnionDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.branches {
      writer.write_tag(1);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:UnionDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.branches {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for UnionDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:UnionDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.branches = option::Option::Some(
            reader
              .read_array(|_r| UnionBranchDescriptor::decode(_r))
              .for_field("UnionDef", "branches")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "UnionDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:UnionDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> UnionDef<'buf> {
  pub fn with_branches(
    mut self,
    value: impl iter::IntoIterator<Item = UnionBranchDescriptor<'buf>>,
  ) -> Self {
    self.branches = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:UnionDef)
}

/// Service definition body.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ServiceDef<'buf> {
  pub methods: option::Option<vec::Vec<MethodDescriptor<'buf>>>,
}

pub type ServiceDefOwned = ServiceDef<'static>;

impl<'buf> ServiceDef<'buf> {
  pub fn into_owned(self) -> ServiceDefOwned {
    ServiceDef {
      methods: self
        .methods
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for ServiceDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:ServiceDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.methods {
      writer.write_tag(1);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:ServiceDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.methods {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for ServiceDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:ServiceDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.methods = option::Option::Some(
            reader
              .read_array(|_r| MethodDescriptor::decode(_r))
              .for_field("ServiceDef", "methods")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "ServiceDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:ServiceDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> ServiceDef<'buf> {
  pub fn with_methods(
    mut self,
    value: impl iter::IntoIterator<Item = MethodDescriptor<'buf>>,
  ) -> Self {
    self.methods = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:ServiceDef)
}

/// Const definition body.
/// Constants are evaluated at parse time. String constants support
/// environment variable substitution:
/// ```bebop
/// const API_KEY : string = $(API_KEY);
/// ```
/// The LiteralValue's `raw_value` preserves `$(API_KEY)` while `string_value`
/// holds the expanded result.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ConstDef<'buf> {
  pub r#type: option::Option<TypeDescriptor<'buf>>,
  pub value: option::Option<LiteralValue<'buf>>,
}

pub type ConstDefOwned = ConstDef<'static>;

impl<'buf> ConstDef<'buf> {
  pub fn into_owned(self) -> ConstDefOwned {
    ConstDef {
      r#type: self.r#type.map(|v| v.into_owned()),
      value: self.value.map(|v| v.into_owned()),
    }
  }
}

impl<'buf> bebop::BebopEncode for ConstDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:ConstDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.r#type {
      writer.write_tag(1);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.value {
      writer.write_tag(2);
      v.encode(writer);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:ConstDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.r#type {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.value {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for ConstDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:ConstDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.r#type =
            option::Option::Some(TypeDescriptor::decode(reader).for_field("ConstDef", "type")?)
        }
        2 => {
          msg.value =
            option::Option::Some(LiteralValue::decode(reader).for_field("ConstDef", "value")?)
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "ConstDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:ConstDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> ConstDef<'buf> {
  pub fn with_type(mut self, value: TypeDescriptor<'buf>) -> Self {
    self.r#type = option::Option::Some(value);
    self
  }
  pub fn with_value(mut self, value: LiteralValue<'buf>) -> Self {
    self.value = option::Option::Some(value);
    self
  }
  // @@bebop_insertion_point(message_scope:ConstDef)
}

/// Decorator parameter definition.
/// Defines the schema for one argument a decorator accepts:
/// ```bebop
/// #decorator(validate) {
///   param min!: float64       // required, no default
///   param max?: float64 = 100 // optional, default 100
///   param mode?: string in ["strict", "lenient"]
/// }
/// ```
#[derive(Debug, Clone, Default, PartialEq)]
pub struct DecoratorParamDef<'buf> {
  pub name: option::Option<borrow::Cow<'buf, str>>,
  /// Description from the `///` comment preceding this param.
  pub description: option::Option<borrow::Cow<'buf, str>>,
  /// Must be a scalar TypeKind.
  pub r#type: option::Option<TypeKind>,
  /// True for required (`!`) params, false for optional (`?`).
  pub required: option::Option<bool>,
  /// Default value for optional params. Absent for required params.
  pub default_value: option::Option<LiteralValue<'buf>>,
  /// Allowed-value constraint from `in [...]`. When non-empty, arguments
  /// must match one of these values.
  pub allowed_values: option::Option<vec::Vec<LiteralValue<'buf>>>,
}

pub type DecoratorParamDefOwned = DecoratorParamDef<'static>;

impl<'buf> DecoratorParamDef<'buf> {
  pub fn into_owned(self) -> DecoratorParamDefOwned {
    DecoratorParamDef {
      name: self.name.map(|v| borrow::Cow::Owned(v.into_owned())),
      description: self.description.map(|v| borrow::Cow::Owned(v.into_owned())),
      r#type: self.r#type,
      required: self.required,
      default_value: self.default_value.map(|v| v.into_owned()),
      allowed_values: self
        .allowed_values
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for DecoratorParamDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:DecoratorParamDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.name {
      writer.write_tag(1);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.description {
      writer.write_tag(2);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.r#type {
      writer.write_tag(3);
      v.encode(writer);
    }
    if let option::Option::Some(v) = self.required {
      writer.write_tag(4);
      writer.write_bool(v);
    }
    if let option::Option::Some(ref v) = self.default_value {
      writer.write_tag(5);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.allowed_values {
      writer.write_tag(6);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:DecoratorParamDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.name {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.description {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.r#type {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(v) = self.required {
      size += bebop::wire_size::tagged_size(mem::size_of::<bool>());
    }
    if let option::Option::Some(ref v) = self.default_value {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.allowed_values {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DecoratorParamDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:DecoratorParamDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.name = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("DecoratorParamDef", "name")?,
          ))
        }
        2 => {
          msg.description = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("DecoratorParamDef", "description")?,
          ))
        }
        3 => {
          msg.r#type =
            option::Option::Some(TypeKind::decode(reader).for_field("DecoratorParamDef", "type")?)
        }
        4 => {
          msg.required = option::Option::Some(
            reader
              .read_bool()
              .for_field("DecoratorParamDef", "required")?,
          )
        }
        5 => {
          msg.default_value = option::Option::Some(
            LiteralValue::decode(reader).for_field("DecoratorParamDef", "default_value")?,
          )
        }
        6 => {
          msg.allowed_values = option::Option::Some(
            reader
              .read_array(|_r| LiteralValue::decode(_r))
              .for_field("DecoratorParamDef", "allowed_values")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "DecoratorParamDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:DecoratorParamDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> DecoratorParamDef<'buf> {
  pub fn with_name(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.name = option::Option::Some(value.into());
    self
  }
  pub fn with_description(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.description = option::Option::Some(value.into());
    self
  }
  pub fn with_type(mut self, value: TypeKind) -> Self {
    self.r#type = option::Option::Some(value);
    self
  }
  pub fn with_required(mut self, value: bool) -> Self {
    self.required = option::Option::Some(value);
    self
  }
  pub fn with_default_value(mut self, value: LiteralValue<'buf>) -> Self {
    self.default_value = option::Option::Some(value);
    self
  }
  pub fn with_allowed_values(
    mut self,
    value: impl iter::IntoIterator<Item = LiteralValue<'buf>>,
  ) -> Self {
    self.allowed_values = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:DecoratorParamDef)
}

/// Decorator definition body.
/// Describes the decorator contract: which elements it can target, what
/// parameters it accepts, and optional Lua source for validate/export.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct DecoratorDef<'buf> {
  /// Bitmask of DecoratorTarget values this decorator may apply to.
  pub targets: option::Option<DecoratorTarget>,
  /// When true, the decorator can appear multiple times on the same target.
  pub allow_multiple: option::Option<bool>,
  pub params: option::Option<vec::Vec<DecoratorParamDef<'buf>>>,
  /// Lua source for validate block. Runs at compile time to reject invalid
  /// usages. Absent when the decorator has no validate block.
  pub validate_source: option::Option<borrow::Cow<'buf, str>>,
  /// Lua source for export block. Produces key-value metadata stored in
  /// DecoratorUsage.export_data. Absent when no export block.
  pub export_source: option::Option<borrow::Cow<'buf, str>>,
}

pub type DecoratorDefOwned = DecoratorDef<'static>;

impl<'buf> DecoratorDef<'buf> {
  pub fn into_owned(self) -> DecoratorDefOwned {
    DecoratorDef {
      targets: self.targets,
      allow_multiple: self.allow_multiple,
      params: self
        .params
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      validate_source: self
        .validate_source
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      export_source: self
        .export_source
        .map(|v| borrow::Cow::Owned(v.into_owned())),
    }
  }
}

impl<'buf> bebop::BebopEncode for DecoratorDef<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:DecoratorDef)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.targets {
      writer.write_tag(1);
      v.encode(writer);
    }
    if let option::Option::Some(v) = self.allow_multiple {
      writer.write_tag(2);
      writer.write_bool(v);
    }
    if let option::Option::Some(ref v) = self.params {
      writer.write_tag(3);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(ref v) = self.validate_source {
      writer.write_tag(4);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.export_source {
      writer.write_tag(5);
      writer.write_string(&v);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:DecoratorDef)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.targets {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(v) = self.allow_multiple {
      size += bebop::wire_size::tagged_size(mem::size_of::<bool>());
    }
    if let option::Option::Some(ref v) = self.params {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(ref v) = self.validate_source {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.export_source {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DecoratorDef<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:DecoratorDef)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.targets = option::Option::Some(
            DecoratorTarget::decode(reader).for_field("DecoratorDef", "targets")?,
          )
        }
        2 => {
          msg.allow_multiple = option::Option::Some(
            reader
              .read_bool()
              .for_field("DecoratorDef", "allow_multiple")?,
          )
        }
        3 => {
          msg.params = option::Option::Some(
            reader
              .read_array(|_r| DecoratorParamDef::decode(_r))
              .for_field("DecoratorDef", "params")?,
          )
        }
        4 => {
          msg.validate_source = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("DecoratorDef", "validate_source")?,
          ))
        }
        5 => {
          msg.export_source = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("DecoratorDef", "export_source")?,
          ))
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "DecoratorDef",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:DecoratorDef)
    result::Result::Ok(msg)
  }
}

impl<'buf> DecoratorDef<'buf> {
  pub fn with_targets(mut self, value: DecoratorTarget) -> Self {
    self.targets = option::Option::Some(value);
    self
  }
  pub fn with_allow_multiple(mut self, value: bool) -> Self {
    self.allow_multiple = option::Option::Some(value);
    self
  }
  pub fn with_params(
    mut self,
    value: impl iter::IntoIterator<Item = DecoratorParamDef<'buf>>,
  ) -> Self {
    self.params = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_validate_source(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.validate_source = option::Option::Some(value.into());
    self
  }
  pub fn with_export_source(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.export_source = option::Option::Some(value.into());
    self
  }
  // @@bebop_insertion_point(message_scope:DecoratorDef)
}

/// Named definition.
/// Container for all definition kinds. The `kind` field selects which body
/// field is populated. Exactly one body is present per definition.
/// The `name` field is the simple identifier from source (`ChatMessage`).
/// The `fqn` includes package and parent scopes (`myapp.Outer.ChatMessage`).
/// Nested definitions (types declared inside struct/message/union bodies)
/// live in the `nested` array. Inline union branch definitions also appear
/// there. Each nested definition has its own FQN encoding the full path.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct DefinitionDescriptor<'buf> {
  pub kind: option::Option<DefinitionKind>,
  /// Simple name as declared in source.
  pub name: option::Option<borrow::Cow<'buf, str>>,
  /// Fully-qualified name including package and parent scopes.
  pub fqn: option::Option<borrow::Cow<'buf, str>>,
  /// Text from preceding `///` comments in source.
  pub documentation: option::Option<borrow::Cow<'buf, str>>,
  pub visibility: option::Option<Visibility>,
  pub decorators: option::Option<vec::Vec<DecoratorUsage<'buf>>>,
  /// Types declared inside this definition's body.
  pub nested: option::Option<vec::Vec<DefinitionDescriptor<'buf>>>,
  pub enum_def: option::Option<EnumDef<'buf>>,
  pub struct_def: option::Option<StructDef<'buf>>,
  pub message_def: option::Option<MessageDef<'buf>>,
  pub union_def: option::Option<UnionDef<'buf>>,
  pub service_def: option::Option<ServiceDef<'buf>>,
  pub const_def: option::Option<ConstDef<'buf>>,
  pub decorator_def: option::Option<DecoratorDef<'buf>>,
}

pub type DefinitionDescriptorOwned = DefinitionDescriptor<'static>;

impl<'buf> DefinitionDescriptor<'buf> {
  pub fn into_owned(self) -> DefinitionDescriptorOwned {
    DefinitionDescriptor {
      kind: self.kind,
      name: self.name.map(|v| borrow::Cow::Owned(v.into_owned())),
      fqn: self.fqn.map(|v| borrow::Cow::Owned(v.into_owned())),
      documentation: self
        .documentation
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      visibility: self.visibility,
      decorators: self
        .decorators
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      nested: self
        .nested
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      enum_def: self.enum_def.map(|v| v.into_owned()),
      struct_def: self.struct_def.map(|v| v.into_owned()),
      message_def: self.message_def.map(|v| v.into_owned()),
      union_def: self.union_def.map(|v| v.into_owned()),
      service_def: self.service_def.map(|v| v.into_owned()),
      const_def: self.const_def.map(|v| v.into_owned()),
      decorator_def: self.decorator_def.map(|v| v.into_owned()),
    }
  }
}

impl<'buf> bebop::BebopEncode for DefinitionDescriptor<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:DefinitionDescriptor)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.kind {
      writer.write_tag(1);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.name {
      writer.write_tag(2);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.fqn {
      writer.write_tag(3);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.documentation {
      writer.write_tag(4);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.visibility {
      writer.write_tag(5);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.decorators {
      writer.write_tag(6);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(ref v) = self.nested {
      writer.write_tag(7);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(ref v) = self.enum_def {
      writer.write_tag(8);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.struct_def {
      writer.write_tag(9);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.message_def {
      writer.write_tag(10);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.union_def {
      writer.write_tag(11);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.service_def {
      writer.write_tag(12);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.const_def {
      writer.write_tag(13);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.decorator_def {
      writer.write_tag(14);
      v.encode(writer);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:DefinitionDescriptor)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.kind {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.name {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.fqn {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.documentation {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.visibility {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.decorators {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(ref v) = self.nested {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(ref v) = self.enum_def {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.struct_def {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.message_def {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.union_def {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.service_def {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.const_def {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.decorator_def {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DefinitionDescriptor<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:DefinitionDescriptor)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.kind = option::Option::Some(
            DefinitionKind::decode(reader).for_field("DefinitionDescriptor", "kind")?,
          )
        }
        2 => {
          msg.name = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("DefinitionDescriptor", "name")?,
          ))
        }
        3 => {
          msg.fqn = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("DefinitionDescriptor", "fqn")?,
          ))
        }
        4 => {
          msg.documentation = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("DefinitionDescriptor", "documentation")?,
          ))
        }
        5 => {
          msg.visibility = option::Option::Some(
            Visibility::decode(reader).for_field("DefinitionDescriptor", "visibility")?,
          )
        }
        6 => {
          msg.decorators = option::Option::Some(
            reader
              .read_array(|_r| DecoratorUsage::decode(_r))
              .for_field("DefinitionDescriptor", "decorators")?,
          )
        }
        7 => {
          msg.nested = option::Option::Some(
            reader
              .read_array(|_r| DefinitionDescriptor::decode(_r))
              .for_field("DefinitionDescriptor", "nested")?,
          )
        }
        8 => {
          msg.enum_def = option::Option::Some(
            EnumDef::decode(reader).for_field("DefinitionDescriptor", "enum_def")?,
          )
        }
        9 => {
          msg.struct_def = option::Option::Some(
            StructDef::decode(reader).for_field("DefinitionDescriptor", "struct_def")?,
          )
        }
        10 => {
          msg.message_def = option::Option::Some(
            MessageDef::decode(reader).for_field("DefinitionDescriptor", "message_def")?,
          )
        }
        11 => {
          msg.union_def = option::Option::Some(
            UnionDef::decode(reader).for_field("DefinitionDescriptor", "union_def")?,
          )
        }
        12 => {
          msg.service_def = option::Option::Some(
            ServiceDef::decode(reader).for_field("DefinitionDescriptor", "service_def")?,
          )
        }
        13 => {
          msg.const_def = option::Option::Some(
            ConstDef::decode(reader).for_field("DefinitionDescriptor", "const_def")?,
          )
        }
        14 => {
          msg.decorator_def = option::Option::Some(
            DecoratorDef::decode(reader).for_field("DefinitionDescriptor", "decorator_def")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "DefinitionDescriptor",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:DefinitionDescriptor)
    result::Result::Ok(msg)
  }
}

impl<'buf> DefinitionDescriptor<'buf> {
  pub fn with_kind(mut self, value: DefinitionKind) -> Self {
    self.kind = option::Option::Some(value);
    self
  }
  pub fn with_name(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.name = option::Option::Some(value.into());
    self
  }
  pub fn with_fqn(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.fqn = option::Option::Some(value.into());
    self
  }
  pub fn with_documentation(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.documentation = option::Option::Some(value.into());
    self
  }
  pub fn with_visibility(mut self, value: Visibility) -> Self {
    self.visibility = option::Option::Some(value);
    self
  }
  pub fn with_decorators(
    mut self,
    value: impl iter::IntoIterator<Item = DecoratorUsage<'buf>>,
  ) -> Self {
    self.decorators = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_nested(
    mut self,
    value: impl iter::IntoIterator<Item = DefinitionDescriptor<'buf>>,
  ) -> Self {
    self.nested = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_enum_def(mut self, value: EnumDef<'buf>) -> Self {
    self.enum_def = option::Option::Some(value);
    self
  }
  pub fn with_struct_def(mut self, value: StructDef<'buf>) -> Self {
    self.struct_def = option::Option::Some(value);
    self
  }
  pub fn with_message_def(mut self, value: MessageDef<'buf>) -> Self {
    self.message_def = option::Option::Some(value);
    self
  }
  pub fn with_union_def(mut self, value: UnionDef<'buf>) -> Self {
    self.union_def = option::Option::Some(value);
    self
  }
  pub fn with_service_def(mut self, value: ServiceDef<'buf>) -> Self {
    self.service_def = option::Option::Some(value);
    self
  }
  pub fn with_const_def(mut self, value: ConstDef<'buf>) -> Self {
    self.const_def = option::Option::Some(value);
    self
  }
  pub fn with_decorator_def(mut self, value: DecoratorDef<'buf>) -> Self {
    self.decorator_def = option::Option::Some(value);
    self
  }
  // @@bebop_insertion_point(message_scope:DefinitionDescriptor)
}

/// Source location for a descriptor element.
/// Maps descriptor elements to source positions and comments. Used for
/// error reporting, documentation extraction, and IDE features.
#[derive(Debug, Clone, Default, PartialEq, Eq, Hash)]
pub struct Location<'buf> {
  /// Path into the descriptor tree. Encoded as pairs of (field_tag, index):
  /// ```
  /// [5, 0]       -> schema.definitions[0]
  /// [5, 0, 1, 2] -> schema.definitions[0].fields[2]
  /// [5, 1, 2, 0] -> schema.definitions[1].members[0]
  /// ```
  /// Field tags correspond to definition body field indices
  /// (StructDef.fields = 1, EnumDef.members = 2, etc.).
  pub path: option::Option<borrow::Cow<'buf, [i32]>>,
  /// Source span as `[start_line, start_col, end_line, end_col]`.
  /// All values 1-based. Columns count characters, tabs advance to
  /// next multiple of 4.
  pub span: option::Option<[i32; 4]>,
  /// Comments on adjacent preceding lines with no blank line separation.
  pub leading_comments: option::Option<borrow::Cow<'buf, str>>,
  /// Comment on the same line after the element or after an opening brace.
  pub trailing_comments: option::Option<borrow::Cow<'buf, str>>,
  /// Comment groups separated from the element by blank lines.
  pub detached_comments: option::Option<vec::Vec<borrow::Cow<'buf, str>>>,
}

pub type LocationOwned = Location<'static>;

impl<'buf> Location<'buf> {
  pub fn into_owned(self) -> LocationOwned {
    Location {
      path: self.path.map(|v| borrow::Cow::Owned(v.into_owned())),
      span: self.span,
      leading_comments: self
        .leading_comments
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      trailing_comments: self
        .trailing_comments
        .map(|v| borrow::Cow::Owned(v.into_owned())),
      detached_comments: self.detached_comments.map(|v| {
        v.into_iter()
          .map(|_e| borrow::Cow::Owned(_e.into_owned()))
          .collect()
      }),
    }
  }
}

impl<'buf> bebop::BebopEncode for Location<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:Location)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.path {
      writer.write_tag(1);
      writer.write_scalar_array::<i32>(&v);
    }
    if let option::Option::Some(ref v) = self.span {
      writer.write_tag(2);
      writer.write_fixed_array::<i32, 4>(&v);
    }
    if let option::Option::Some(ref v) = self.leading_comments {
      writer.write_tag(3);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.trailing_comments {
      writer.write_tag(4);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.detached_comments {
      writer.write_tag(5);
      writer.write_array(&v, |_w, _el| _w.write_string(_el));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:Location)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.path {
      size += bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| {
        (mem::size_of::<i32>())
      }));
    }
    if let option::Option::Some(ref v) = self.span {
      size += bebop::wire_size::tagged_size(4usize * (mem::size_of::<i32>()));
    }
    if let option::Option::Some(ref v) = self.leading_comments {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.trailing_comments {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.detached_comments {
      size += bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| {
        bebop::wire_size::string_size(_el.len())
      }));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for Location<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:Location)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.path = option::Option::Some(
            reader
              .read_scalar_array::<i32>()
              .for_field("Location", "path")?,
          )
        }
        2 => {
          msg.span = option::Option::Some(
            reader
              .read_fixed_array::<i32, 4>()
              .for_field("Location", "span")?,
          )
        }
        3 => {
          msg.leading_comments = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("Location", "leading_comments")?,
          ))
        }
        4 => {
          msg.trailing_comments = option::Option::Some(borrow::Cow::Borrowed(
            reader
              .read_str()
              .for_field("Location", "trailing_comments")?,
          ))
        }
        5 => {
          msg.detached_comments = option::Option::Some(
            reader
              .read_array(|_r| result::Result::Ok(borrow::Cow::Borrowed(_r.read_str()?)))
              .for_field("Location", "detached_comments")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "Location",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:Location)
    result::Result::Ok(msg)
  }
}

impl<'buf> Location<'buf> {
  pub fn with_path(mut self, value: impl convert::Into<borrow::Cow<'buf, [i32]>>) -> Self {
    self.path = option::Option::Some(value.into());
    self
  }
  pub fn with_span(mut self, value: [i32; 4]) -> Self {
    self.span = option::Option::Some(value);
    self
  }
  pub fn with_leading_comments(
    mut self,
    value: impl convert::Into<borrow::Cow<'buf, str>>,
  ) -> Self {
    self.leading_comments = option::Option::Some(value.into());
    self
  }
  pub fn with_trailing_comments(
    mut self,
    value: impl convert::Into<borrow::Cow<'buf, str>>,
  ) -> Self {
    self.trailing_comments = option::Option::Some(value.into());
    self
  }
  pub fn with_detached_comments(
    mut self,
    value: impl iter::IntoIterator<Item = impl convert::Into<borrow::Cow<'buf, str>>>,
  ) -> Self {
    self.detached_comments = option::Option::Some(value.into_iter().map(|_e| _e.into()).collect());
    self
  }
  // @@bebop_insertion_point(message_scope:Location)
}

/// Source code info for a schema.
/// One Location entry per locatable element. Includes definitions, fields,
/// enum members, union branches, and service methods. Not every element
/// has a location.
#[derive(Debug, Clone, Default, PartialEq, Eq, Hash)]
pub struct SourceCodeInfo<'buf> {
  pub locations: option::Option<vec::Vec<Location<'buf>>>,
}

pub type SourceCodeInfoOwned = SourceCodeInfo<'static>;

impl<'buf> SourceCodeInfo<'buf> {
  pub fn into_owned(self) -> SourceCodeInfoOwned {
    SourceCodeInfo {
      locations: self
        .locations
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for SourceCodeInfo<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:SourceCodeInfo)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.locations {
      writer.write_tag(1);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:SourceCodeInfo)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.locations {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for SourceCodeInfo<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:SourceCodeInfo)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.locations = option::Option::Some(
            reader
              .read_array(|_r| Location::decode(_r))
              .for_field("SourceCodeInfo", "locations")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "SourceCodeInfo",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:SourceCodeInfo)
    result::Result::Ok(msg)
  }
}

impl<'buf> SourceCodeInfo<'buf> {
  pub fn with_locations(mut self, value: impl iter::IntoIterator<Item = Location<'buf>>) -> Self {
    self.locations = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:SourceCodeInfo)
}

/// Descriptor for a single .bop source file.
/// Each source file produces one SchemaDescriptor. Definitions are
/// topologically sorted: every type appears after the types it depends on.
/// Generators can emit types in array order without forward declarations.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct SchemaDescriptor<'buf> {
  /// File path as provided to the compiler.
  pub path: option::Option<borrow::Cow<'buf, str>>,
  /// Package declaration from source. Absent when no package is declared.
  pub package: option::Option<borrow::Cow<'buf, str>>,
  pub edition: option::Option<Edition>,
  /// Import paths in source declaration order.
  pub imports: option::Option<vec::Vec<borrow::Cow<'buf, str>>>,
  /// All definitions in topological dependency order.
  pub definitions: option::Option<vec::Vec<DefinitionDescriptor<'buf>>>,
  /// Source code info. Only present when requested during compilation.
  pub source_code_info: option::Option<SourceCodeInfo<'buf>>,
}

pub type SchemaDescriptorOwned = SchemaDescriptor<'static>;

impl<'buf> SchemaDescriptor<'buf> {
  pub fn into_owned(self) -> SchemaDescriptorOwned {
    SchemaDescriptor {
      path: self.path.map(|v| borrow::Cow::Owned(v.into_owned())),
      package: self.package.map(|v| borrow::Cow::Owned(v.into_owned())),
      edition: self.edition,
      imports: self.imports.map(|v| {
        v.into_iter()
          .map(|_e| borrow::Cow::Owned(_e.into_owned()))
          .collect()
      }),
      definitions: self
        .definitions
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
      source_code_info: self.source_code_info.map(|v| v.into_owned()),
    }
  }
}

impl<'buf> bebop::BebopEncode for SchemaDescriptor<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:SchemaDescriptor)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.path {
      writer.write_tag(1);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.package {
      writer.write_tag(2);
      writer.write_string(&v);
    }
    if let option::Option::Some(ref v) = self.edition {
      writer.write_tag(3);
      v.encode(writer);
    }
    if let option::Option::Some(ref v) = self.imports {
      writer.write_tag(4);
      writer.write_array(&v, |_w, _el| _w.write_string(_el));
    }
    if let option::Option::Some(ref v) = self.definitions {
      writer.write_tag(5);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    if let option::Option::Some(ref v) = self.source_code_info {
      writer.write_tag(6);
      v.encode(writer);
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:SchemaDescriptor)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.path {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.package {
      size += bebop::wire_size::tagged_size(bebop::wire_size::string_size(v.len()));
    }
    if let option::Option::Some(ref v) = self.edition {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    if let option::Option::Some(ref v) = self.imports {
      size += bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| {
        bebop::wire_size::string_size(_el.len())
      }));
    }
    if let option::Option::Some(ref v) = self.definitions {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    if let option::Option::Some(ref v) = self.source_code_info {
      size += bebop::wire_size::tagged_size(v.encoded_size());
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for SchemaDescriptor<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:SchemaDescriptor)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.path = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("SchemaDescriptor", "path")?,
          ))
        }
        2 => {
          msg.package = option::Option::Some(borrow::Cow::Borrowed(
            reader.read_str().for_field("SchemaDescriptor", "package")?,
          ))
        }
        3 => {
          msg.edition =
            option::Option::Some(Edition::decode(reader).for_field("SchemaDescriptor", "edition")?)
        }
        4 => {
          msg.imports = option::Option::Some(
            reader
              .read_array(|_r| result::Result::Ok(borrow::Cow::Borrowed(_r.read_str()?)))
              .for_field("SchemaDescriptor", "imports")?,
          )
        }
        5 => {
          msg.definitions = option::Option::Some(
            reader
              .read_array(|_r| DefinitionDescriptor::decode(_r))
              .for_field("SchemaDescriptor", "definitions")?,
          )
        }
        6 => {
          msg.source_code_info = option::Option::Some(
            SourceCodeInfo::decode(reader).for_field("SchemaDescriptor", "source_code_info")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "SchemaDescriptor",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:SchemaDescriptor)
    result::Result::Ok(msg)
  }
}

impl<'buf> SchemaDescriptor<'buf> {
  pub fn with_path(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.path = option::Option::Some(value.into());
    self
  }
  pub fn with_package(mut self, value: impl convert::Into<borrow::Cow<'buf, str>>) -> Self {
    self.package = option::Option::Some(value.into());
    self
  }
  pub fn with_edition(mut self, value: Edition) -> Self {
    self.edition = option::Option::Some(value);
    self
  }
  pub fn with_imports(
    mut self,
    value: impl iter::IntoIterator<Item = impl convert::Into<borrow::Cow<'buf, str>>>,
  ) -> Self {
    self.imports = option::Option::Some(value.into_iter().map(|_e| _e.into()).collect());
    self
  }
  pub fn with_definitions(
    mut self,
    value: impl iter::IntoIterator<Item = DefinitionDescriptor<'buf>>,
  ) -> Self {
    self.definitions = option::Option::Some(value.into_iter().collect());
    self
  }
  pub fn with_source_code_info(mut self, value: SourceCodeInfo<'buf>) -> Self {
    self.source_code_info = option::Option::Some(value);
    self
  }
  // @@bebop_insertion_point(message_scope:SchemaDescriptor)
}

/// Root container for compiled schemas.
/// Written to .bop.bin files or passed to code generators. Schemas are
/// ordered so imported schemas appear before schemas that import them.
/// Processing schemas[0..N] in order encounters dependencies before
/// they are referenced.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct DescriptorSet<'buf> {
  pub schemas: option::Option<vec::Vec<SchemaDescriptor<'buf>>>,
}

pub type DescriptorSetOwned = DescriptorSet<'static>;

impl<'buf> DescriptorSet<'buf> {
  pub fn into_owned(self) -> DescriptorSetOwned {
    DescriptorSet {
      schemas: self
        .schemas
        .map(|v| v.into_iter().map(|_e| _e.into_owned()).collect()),
    }
  }
}

impl<'buf> bebop::BebopEncode for DescriptorSet<'buf> {
  fn encode(&self, writer: &mut bebop::BebopWriter) {
    // @@bebop_insertion_point(encode_start:DescriptorSet)
    let pos = writer.reserve_message_length();
    // NOTE: Deprecated fields are currently encoded and decoded like normal fields.
    // The GRAMMAR.md spec says deprecated message fields should be skipped during
    // encoding and decoding. The C plugin (plugins/c/src/generator.c:3446) skips
    // them on encode/size but still decodes them. The Swift plugin encodes them
    // normally. This behavior should be revisited once the spec intent is clarified.
    if let option::Option::Some(ref v) = self.schemas {
      writer.write_tag(1);
      writer.write_array(&v, |_w, _el| _el.encode(_w));
    }
    writer.write_end_marker();
    writer.fill_message_length(pos);
    // @@bebop_insertion_point(encode_end:DescriptorSet)
  }

  fn encoded_size(&self) -> usize {
    let mut size = bebop::wire_size::WIRE_MESSAGE_BASE_SIZE;
    if let option::Option::Some(ref v) = self.schemas {
      size +=
        bebop::wire_size::tagged_size(bebop::wire_size::array_size(v, |_el| _el.encoded_size()));
    }
    size
  }
}

impl<'buf> bebop::BebopDecode<'buf> for DescriptorSet<'buf> {
  #[inline]
  fn decode(reader: &mut bebop::BebopReader<'buf>) -> result::Result<Self, bebop::DecodeError> {
    // @@bebop_insertion_point(decode_start:DescriptorSet)
    let length = reader.read_message_length()? as usize;
    let end = reader.position() + length;
    let mut msg = <Self as default::Default>::default();

    while reader.position() < end {
      let tag = reader.read_tag()?;
      if tag == 0 {
        break;
      }
      match tag {
        1 => {
          msg.schemas = option::Option::Some(
            reader
              .read_array(|_r| SchemaDescriptor::decode(_r))
              .for_field("DescriptorSet", "schemas")?,
          )
        }
        tag => {
          return result::Result::Err(bebop::DecodeError::InvalidField {
            type_name: "DescriptorSet",
            tag,
          });
        }
      }
    }
    // @@bebop_insertion_point(decode_end:DescriptorSet)
    result::Result::Ok(msg)
  }
}

impl<'buf> DescriptorSet<'buf> {
  pub fn with_schemas(
    mut self,
    value: impl iter::IntoIterator<Item = SchemaDescriptor<'buf>>,
  ) -> Self {
    self.schemas = option::Option::Some(value.into_iter().collect());
    self
  }
  // @@bebop_insertion_point(message_scope:DescriptorSet)
}

// @@bebop_insertion_point(eof)
